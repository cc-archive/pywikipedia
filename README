These files are not usable alone.

You should first do a checkout of pywikipedia, tested with r5140.

$ http://svn.wikimedia.org/svnroot/pywikipedia/trunk/pywikipedia

Then, symlink the families entry in.

Then, take user-config.py.templ and modify it into user-config.py.  Symlink
that into the pywikipedia directory also.

Then:

$ export PYWIKIPEDIA_PATH=/path/to/checkout/of/pywikipedia/
$ python $PYWIKIPEDIA_PATH/login.py

Then you can run these things in here.
